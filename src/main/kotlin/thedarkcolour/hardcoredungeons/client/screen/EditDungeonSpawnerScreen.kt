package thedarkcolour.hardcoredungeons.client.screen
/*

import com.mojang.blaze3d.matrix.MatrixStack
import net.minecraft.client.gui.screen.inventory.ContainerScreen
import net.minecraft.client.gui.widget.button.Button
import net.minecraft.entity.player.PlayerInventory
import net.minecraft.util.text.StringTextComponent
import net.minecraft.util.text.TranslationTextComponent
import thedarkcolour.hardcoredungeons.container.EditDungeonSpawnerContainer
import thedarkcolour.hardcoredungeons.registry.HBlocks

class EditDungeonSpawnerScreen(screenContainer: EditDungeonSpawnerContainer?, inv: PlayerInventory?) : ContainerScreen<EditDungeonSpawnerContainer>(screenContainer, inv, TranslationTextComponent(HBlocks.DUNGEON_SPAWNER.descriptionId)) {
    private var saveButton: Button? = null

    override fun init() {
        getMinecraft().keyboardHandler.setSendRepeatsToGui(true)
        saveButton = addButton(Button(width / 2 - 4 - 150, 210, 150, 20, SAVE_TEXT) { button ->
            button.active = false
        })
    }

    override fun renderBg(matrixStack: MatrixStack, partialTicks: Float, x: Int, y: Int) {
        TODO("not implemented")
    }

    companion object {
        val SAVE_TEXT = StringTextComponent("Save changes")
    }
}*/
